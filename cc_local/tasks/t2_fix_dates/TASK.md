O loader em src/loader.py esta interpretando datas brasileiras (DD/MM/YYYY) como americanas e
tambem quebra em linhas com data vazia. Corrija `load_orders` para que os testes em tests/ passem.
Nao altere os testes. Rode `pytest -q`.
