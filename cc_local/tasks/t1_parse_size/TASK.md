Implemente a funcao `parse_size(text: str) -> int` em src/sizes.py que converte strings como
"512B", "1.5KB", "2MB", "3 GB", "1TB" (case-insensitive, espaco opcional, base 1024) para bytes inteiros.
Entrada invalida deve levantar ValueError. Rode `pytest -q` e garanta que todos os testes passam.
